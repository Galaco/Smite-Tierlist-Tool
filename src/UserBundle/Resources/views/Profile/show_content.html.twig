{% trans_default_domain 'UserBundle' %}

<div class="segment">
	<h2 class="segment-title">Registration information</h2>
    <p>{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
    <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
</div>

